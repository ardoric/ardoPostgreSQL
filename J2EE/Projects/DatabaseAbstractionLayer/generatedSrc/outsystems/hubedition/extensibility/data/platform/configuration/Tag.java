/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

package outsystems.hubedition.extensibility.data.platform.configuration;

import java.nio.*;
import java.nio.charset.*;
import java.text.*;
import java.util.*;
import outsystems.hubedition.extensibility.data.*;
import outsystems.hubedition.extensibility.data.platform.*;
import outsystems.hubedition.util.*;
import outsystems.hubedition.util.delegates.*;
import java.math.BigDecimal;
import java.util.Map;


public enum Tag implements IEnum {
    //Block tags
    SCRIPT_VERSION,
    VERSION_UPGRADE,
    INVARIANT_INDEX,
    COLUMN_NOT_EXISTS,
    COLUMN_EXISTS,
    BEGIN_STATEMENT,
    END_STATEMENT,
    TABLE_NOT_EXISTS,
    TABLE_EXISTS,
    END_IGNORING_BLOCK,
    TOUCH_VERSION,
    CHECK,
    
    //Statement tags
    DROP_COLUMN,
    SPECIFIC_UPGRADE;
    
    public static final TypeInformation<Tag> TypeInfo = TypeInformation.get(Tag.class);
    public int getIntValue() {
        return ordinal();
    }
    
    public static String[] names() {
        return EnumUtils.getNames(values());
    }
    
    public static Tag getDefaultValue() {
        return SCRIPT_VERSION;
    }
    
    private static Map<Integer, Tag> intToEnum;
    private static Map<Integer, Tag> getIntToEnum() {
        if (intToEnum == null) {
            intToEnum = EnumUtils.getIntToEnumValueMap(values());
        }
        return intToEnum;
    }
    
    private static Map<String, Tag> lowerCaseNameToEnum;
    private static Map<String, Tag> getLowerCaseNameToEnum() {
        if (lowerCaseNameToEnum == null) {
            lowerCaseNameToEnum = EnumUtils.getNameToEnumValueMap(values(), /*lowerCase*/true);
        }
        return lowerCaseNameToEnum;
    }
    
    private static Map<String, Tag> nameToEnum;
    private static Map<String, Tag> getNameToEnum() {
        if (nameToEnum == null) {
            nameToEnum = EnumUtils.getNameToEnumValueMap(values(), /*lowerCase*/false);
        }
        return nameToEnum;
    }
    
    public static Tag valueOf(int value) {
        Tag result = getIntToEnum().get(value);
        if (result == null) {
            throw new IllegalArgumentException("No enum const class Tag with int value " + value);
        }
        return result;
    }
    
    
    public static Tag valueOf(String value, boolean ignoreCase) {
        if (!ignoreCase) {
            return valueOf(value);
        }
        Tag result = getLowerCaseNameToEnum().get(value.toLowerCase());
        if (result == null) {
            throw new IllegalArgumentException("No enum const class Tag." + value);
        }
        return result;
    }
    
    
    public static boolean isDefined(String value) {
        return getNameToEnum().containsKey(value);
    }
    
    public static boolean isDefined(int value) {
        return getIntToEnum().containsKey(value);
    }
}
