/* 
 This source code (the "Generated Software") is generated by the OutSystems Platform 
 and is licensed by OutSystems (http://www.outsystems.com) to You solely for testing and evaluation 
 purposes, unless You and OutSystems have executed a specific agreement covering the use terms and 
 conditions of the Generated Software, in which case such agreement shall apply. 
*/

package outsystems.hubedition.extensibility.data.platform.ddlservice;

import java.text.*;
import java.util.*;
import outsystems.hubedition.extensibility.data.*;
import outsystems.hubedition.extensibility.data.platform.*;
import outsystems.hubedition.util.*;
import outsystems.hubedition.util.delegates.*;
import java.math.BigDecimal;
import java.util.Map;



/**
 *	Set of permissions that can be granted.
 */
public enum Permissions implements IEnum {
    Select(1),
    Insert(2),
    Update(4),
    Delete(8);
    
    public static final TypeInformation<EnumSet<Permissions>> TypeInfo = TypeInformation.get(EnumSet.class, TypeInformation.get(Permissions.class));
    public static final EnumSet<Permissions> None = EnumSet.noneOf(Permissions.class);
    private int value;
    Permissions(int value) {
        this.value = value;
    }
    
    public int getIntValue() {
        return value;
    }
    
    public static String[] names() {
        return EnumUtils.getNames(values());
    }
    
    public static EnumSet<Permissions> getDefaultValue() {
        return None;
    }
    
    private static Map<Integer, Permissions> intToEnum;
    private static Map<Integer, Permissions> getIntToEnum() {
        if (intToEnum == null) {
            intToEnum = EnumUtils.getIntToEnumValueMap(values());
        }
        return intToEnum;
    }
    
    private static Map<String, Permissions> lowerCaseNameToEnum;
    private static Map<String, Permissions> getLowerCaseNameToEnum() {
        if (lowerCaseNameToEnum == null) {
            lowerCaseNameToEnum = EnumUtils.getNameToEnumValueMap(values(), /*lowerCase*/true);
        }
        return lowerCaseNameToEnum;
    }
    
    private static Map<String, Permissions> nameToEnum;
    private static Map<String, Permissions> getNameToEnum() {
        if (nameToEnum == null) {
            nameToEnum = EnumUtils.getNameToEnumValueMap(values(), /*lowerCase*/false);
        }
        return nameToEnum;
    }
    
    public static Permissions valueOf(int value) {
        Permissions result = getIntToEnum().get(value);
        if (result == null) {
            throw new IllegalArgumentException("No enum const class Permissions with int value " + value);
        }
        return result;
    }
    
    
    public static Permissions valueOf(String value, boolean ignoreCase) {
        if (!ignoreCase) {
            return valueOf(value);
        }
        Permissions result = getLowerCaseNameToEnum().get(value.toLowerCase());
        if (result == null) {
            throw new IllegalArgumentException("No enum const class Permissions." + value);
        }
        return result;
    }
    
    
    public static boolean isDefined(String value) {
        return getNameToEnum().containsKey(value);
    }
    
    public static boolean isDefined(int value) {
        return getIntToEnum().containsKey(value);
    }
}
